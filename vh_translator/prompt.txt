These strings are an app-store LISTING (Google Play / App Store / Microsoft Store) for a consumer VPN app — marketing copy, not UI labels. Translate naturally and persuasively for the target market. This app (VpnHood! CONNECT) is the ready-to-use consumer VPN: it ships with built-in public servers, connects with one tap, and offers an optional premium upgrade — unlike VpnHood! CLIENT, which requires the user's own server key. Keep that ready-to-use, no-setup tone.

- The brand name "VpnHood" — alone, as "VpnHood!", or in product names like "VpnHood! CONNECT", "VpnHood! CLIENT", "VpnHood! STORE", "VpnHood! ENGINE" — never changes: never translate or transliterate it; always keep it in Latin script.
- Platform and company names (GitHub, Google Play, App Store, Windows, Android, iOS, .NET) never change.
- Keep URLs and email addresses exactly as written.
- Translate feature names the way the app's own UI would, and use them consistently: Cloak Mode, Split Tunneling, Kill Switch, Private DNS, Custom DNS, Always-On, Quick Launch.
- Keep the paragraph structure, blank lines, and the ▶ ★ • ✔ list markers exactly as in the source.
- Stores enforce HARD character limits per key; a longer translation fails the release, so shorten rather than exceed. Count the characters of every limited value before returning it and aim at least 10% BELOW its limit — a shorter phrase always beats a rejected release. Invisible characters (LRM/RLM direction marks) count toward the limit too — use them only when strictly required for correct display:
  - "android.title": 30 characters maximum — aim for 28 or fewer; keep it as the brand name plus a VERY short tagline, and drop the tagline entirely rather than exceed the limit
  - "android.short_description": 80 characters maximum
  - "android.full_description": 4000 characters maximum — translations run longer than English, so condense freely (merge or shorten bullets) and aim under 3,600
  - "ios.subtitle": 30 characters maximum
  - "ios.keywords": 100 characters maximum — a comma-separated keyword list with no space after commas, not a sentence
  - "ios.promotional_text": 170 characters maximum
  - "ios.description": 4000 characters maximum — translations run longer than English, so condense freely (merge or shorten bullets) and aim under 3,600
