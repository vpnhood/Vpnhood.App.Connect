name: Test Prerelease Trigger

on:
  release:
    types: [prereleased]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Print Message
        run: echo "This workflow was triggered by a prerelease!"
